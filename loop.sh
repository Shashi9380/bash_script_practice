#!/bin/bash

# url="$1"

# if [[ -z "$url" || $# -le 0 ]]; then
#   echo "please put url"
#   echo "usage: $0 <url>"
#   exit 1
# fi

# for i in {1..5}; do
#   status_code=$(curl -s -o /dev/null -w "%{http_code}\n" "$url")
#   status_code=$(curl -s -o /dev/null -w "%{http_code}\n" "$url")
#   echo "$status_code"
#   if [[ $status_code -eq 200 || $status_code -eq 100 || $status_code -eq 300 ]]; then
#     echo "$url is working"
#     exit 1
#   else
#     echo "checking every 5sec"
#     sleep 5
#   fi
# done






